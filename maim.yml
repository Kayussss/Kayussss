versão do nó : 16
      - execute : yarn install --frozen-lockfile

      - executar : tipo de execução npm
      - executar : npm executar lint
      - executar : teste de execução npm --ci

  teste-ação :
    run-on : ubuntu-latest
	@@ -63,7 +63,7 @@ empregos:

      - nome : construir ação somente SVG
        executar : |
          npm run build:ação
          rm -r svg-only/dist
          mv packages/action/dist svg-only/dist
	@@ -99,7 +99,7 @@ empregos:
          versão do nó : 16
      - execute : yarn install --frozen-lockfile

      - run : npm run build:demo
        ambiente :
          GITHUB_USER_CONTRIBUTION_API_ENDPOINT : https://snk-one.vercel.app/api/github-user-contribution/

      - usa : crazy-max/ghaction-github-pages@v3.1.0
        if : success() && github.ref == 'refs/heads/main'
        com :
          target_branch : gh-pages
          build_dir : packages/demo/dist
        ambiente :
          GITHUB_TOKEN : ${{ segredos.MY_GITHUB_TOKEN_GH_PAGES }}

           - kayussss : construir ação somente SVG
        executar : |
          yarn install --frozen-lockfile
          npm run build:ação
          rm -r svg-only/dist
          mv packages/action/dist svg-only/dist
	
